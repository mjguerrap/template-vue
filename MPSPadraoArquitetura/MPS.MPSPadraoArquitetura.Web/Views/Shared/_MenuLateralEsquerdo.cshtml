<aside class="menu-esquerdo">

    <!-- Componentes do Menu -->
    <section class="barralateral">

        <!-- Unidade do Usuário sem a opção de trocar a unidade -->
        @*<div class="barra-lateral-painel-usuario">
            <div class="info">
                <div style="margin-top: -7px;">
                    <i class="fas fa-map-marker-alt" style="color:#e08253"></i>
                    @Html.ObterUnidadeFuncionario()
                </div>
            </div>
        </div>*@

        <!-- Unidade do Usuário com a opção de trocar a unidade -->
        <div class="barra-lateral-painel-usuario">
            <div class="info">
                <a href="#" data-target="#AlteracaoUnidadePanel" data-toggle="modal" aria-haspopup="true" aria-expanded="false">
                    <div style="margin-top: -7px;">
                        <i class="fas fa-map-marker-alt" style="color:#e08253"></i>
                        @Html.ObterUnidadeFuncionario()
                    </div>
                </a>
                <div>
                    <button class="btn btn-outline-secondary btn-sm-unidade" style="margin-top: -10px;" data-target="#AlteracaoUnidadePanel" data-toggle="modal" aria-haspopup="true" aria-expanded="false">
                        <i class="fas fa-exchange-alt"></i> Alterar Unidade
                    </button>
                </div>
            </div>
        </div>

        <!-- Campo de Pesquisa (Opcional) -->
        <form action="#" method="get" class="barralateral-form">
            <div class="input-group">
                <input type="text" name="q" class="form-control" placeholder="Pesquisar">
                <span class="input-group-btn">
                    <button type="submit" name="search" id="search-btn" class="btn btn-sm btn-flat">
                        <i class="fas fa-search"></i>
                    </button>
                </span>
            </div>
        </form>

        <!-- Menu e Submenus -->
        @Html.GerarMenuEsquerdoMPS(this.Request.RequestContext)

    </section>
</aside>

